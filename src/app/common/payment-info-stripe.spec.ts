import { PaymentInfoStripe } from './payment-info-stripe';

describe('PaymentInfoStripe', () => {
  it('should create an instance', () => {
    expect(new PaymentInfoStripe()).toBeTruthy();
  });
});
